# SmartVet

QR Code-Based Portable Pet Health E-Card System for Veterinary Record Management

## Setup

```cmd
composer install

npm install

php artisan key:generate

php artisan migrate

php artisan db:seed

php artisan storage:link
```

Add 2 more terminals and run this 2 commands in each terminal

```cmd
php artisan serve

npm run dev
```
